# Maestro session state (local only, never committed)
.maestro/state.local.md
.maestro/logs/
.maestro/instances/
.maestro/locks/
.maestro/recon/
.maestro/sparc/
.maestro/*.lock
.claude/
temp/
.env
node_modules/

# Credential vault — plaintext MUST NOT be committed (encrypted .age is safe)
.maestro/vault.yaml
